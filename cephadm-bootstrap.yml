---
- name: bootstrap the cluster
  hosts: admin[0]
  become: true
  gather_facts: false
  tasks:
    - name: bootstrap initial cluster
      cephadm_bootstrap:
        mon_ip: "{{ monitor_address }}"
